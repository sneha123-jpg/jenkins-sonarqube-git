pipeline {
    agent any

    stages {

        stage ("Pull") {
            steps {
                git 'https://github.com/sneha123-jpg/sonar-jenkin.git'
            }
        }

        stage ("Build") {
            steps {
                sh 'mvn clean package'
            }
        }

        stage ("Test") {
            steps {
                echo "test success"

               withSonarQubeEnv(credentialsId: 'my-sonar-token',installationName: 'my-sonar') {
     

 sh '''
                mvn clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
                -Dsonar.projectKey=java-students \
                -Dsonar.projectName=java-students \...
                                 
            }
        }

        stage ("Quality Gate") {
            steps {
                echo "quality success"
            }
        }

        stage ("Deploy") {
            steps {
                echo "deploy success"
            }
        }
    }
}
